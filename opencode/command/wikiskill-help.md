---
description: Show the detailed WikiSkill guide — how the framework works, commands, automation, costs
---

Run `python3 .wikiskill/bin/wikiskill.py guide` and `python3
.wikiskill/bin/wikiskill.py status`. Present the guide in the conversation
language, keeping all substance (three layers, the loop, gating, automation
knobs, token accounting, command list — note the opencode commands are
`/wikiskill-*`), then add a one-paragraph "where this project is right now"
from the status and the single most useful next action.
